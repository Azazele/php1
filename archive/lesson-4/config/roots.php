<?
$sep = DIRECTORY_SEPARATOR;
define('PUBLIC_ROOT', __DIR__ . $sep . '..' . $sep . 'public' . $sep);
define('API_ROOT', __DIR__ . $sep . '..' . $sep . 'API' . $sep);
define('CONFIG_ROOT', __DIR__ . $sep . '..' . $sep . 'config' . $sep);
define('MAIN_ROOT', __DIR__ . $sep . '..' . $sep);